# Curriculum Governance Layer

This directory is reserved for governance artifacts and policies that sit outside the existing curriculum reasoning and data loader.

It is intentionally additive and does not modify:

- `data/` curriculum files
- `engine/catalogue.py` catalogue loader
- existing scope logic and reasoning rules
- current application code paths

Use this layer for manifest baselines, operational templates, and governance metadata.
